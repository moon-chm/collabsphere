kotlin.code.style=official
# Give the build's own JVM enough headroom to compile without OOM-ing inside a constrained
# Docker build stage (this repo's Dockerfile already builds with --no-daemon).
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
